import React from 'react';
import ReactDOM from 'react-dom';
import Button from '@material-ui/core/Button';
import CounterButtons from './views/Counter/styles'

ReactDOM.render(
    <CounterButtons />,
    document.getElementById('root')
  );