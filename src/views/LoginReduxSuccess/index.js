import React from 'react';
import Typography from '@material-ui/core/Typography';

const LoginReduxSuccess = ({classes}) => (
        <Typography>
          Success!
        </Typography>
)

export default LoginReduxSuccess;